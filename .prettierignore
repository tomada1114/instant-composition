# Build output and caches
dist/
coverage/
node_modules/
cdk.out/

# Data under test, not source: a fixture is committed precisely because it is
# malformed, and formatting it would remove the defect a test asserts on.
tests/fixtures/

# Generated by tooling, not hand-formatted
pnpm-lock.yaml

# Generated mirror of .agents/skills/ (`pnpm agents:sync`); the real files are
# formatted at their real path, and reformatting the copy would only drift it.
.claude/skills/

# Full working copies created by agent sessions; formatted in their own checkout.
.claude/worktrees/

# Generated from packages/contracts/openapi.json by `pnpm web:client`, and held
# byte for byte to the generator's output by tests/web-openapi-client.test.ts;
# reformatting it would be a difference that test reports.
apps/web/src/openapi/
